class Solution:
    def singleNumber(self, nums: List[int]) -> int:

        hm = {}

        for num in nums:
            if num in hm:
                hm[num] += 1
            else:
                hm[num] = 1

        for key, value in hm.items():
            if value == 1:
                return key
