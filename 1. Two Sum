class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hm = {}

        for num in range(len(nums)):
            sec = target - nums[num]
            if sec in hm:
                return [hm[sec], num]
            else:
                hm.update({nums[num]: num})
