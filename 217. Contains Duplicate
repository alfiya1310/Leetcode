class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        hm = {}

        for num in range(len(nums)):
             if nums[num] in hm:
                 return True
             else:
                 hm.update({nums[num]: num})

        return False
OR

# class Solution:
#    def containsDuplicate(self, nums: List[int]) -> bool:
#        nums2 = set(nums)
#        return len(nums) != len(nums2)
