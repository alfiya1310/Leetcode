class Solution:
    def findLengthOfLCIS(self, nums: List[int]) -> int:
        l = 0
        maxx = 0

        for r in range(len(nums)):
            if nums[r - 1] >= nums[r]:
                l = r

            maxx = max(maxx, r - l + 1)

        return maxx
